# The port we listen to
httpPort=8181

# Configuration of database
persistenceUnitName=acrs-pgsql

#baseUrl=http://acrs.org/web/guest
#baseUrl=http://acrs-uat.metadata.net/web/guest
baseUrl=http://localhost:8080/web/guest

# Configuration of outgoing email
#
# If the submissionEmailAddress is missing or empty, the feature
# will be disabled (which is the default).
#
# Note that the submission form is not protected, i.e. people can
# use it to send arbitrary data to the address configured here.
emailServer=smtp.uq.edu.au
serverProxyName=proxy.itee.uq.edu.au

paypalVerificationAddress=http://www.sandbox.paypal.com/cgi-bin/webscr